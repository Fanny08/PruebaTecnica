import './Spinner.scss';

const Spinner = () => {
    return (
        <div className="Spinner">
            <i className="fas fa-atom fa-spin fa-5x"></i>
        </div>
    )
}

export default Spinner;